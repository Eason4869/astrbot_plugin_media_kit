# 更新日志

## v1.1.3

- 🐛 **修复 B站（含直播）偶发解析失败、日志错误信息为空**：直播解析器对 `b23.tv` 短链是「乐观认领后再展开确认」，但展开请求一旦发生**超时 / 连接错误**（裸 `asyncio.TimeoutError`，`str()` 为空），此前未被捕获、绕过了 SkipParse 回落，直接变成一条「错误为空」的硬失败，导致普通视频短链无法回落到 B站视频解析器。现把展开期的任何网络异常统一转为 `SkipParse`，确保非直播链接可靠回落；直播间信息接口的网络错误也转为带明确信息的异常，不再出现空错误。
- ⚡ **回落复用 b23 展开结果**：直播解析器确认短链指向视频 / 动态时，会把已展开结果缓存到共享会话，回落后的 B站视频解析器直接复用，不再重复发起一次重定向请求（少一次网络往返与失败点）。
- 新增 / 补充相关离线单测（展开超时转 SkipParse、回落成功、展开结果缓存读写与容错）。

## v1.1.2

- 🔖 版本号变更，无功能改动（对齐发布版本号）。

## v1.1.1

- 🐛 **修复 b23.tv 视频短链（及小程序卡片）解析失败**：v1.1.0 让直播解析器优先认领所有 b23 短链，但解析器对「指向普通视频/动态」的短链抛 `SkipParse` 后**直接丢弃、未回落**给 B站视频解析器，导致视频短链与小程序卡片无任何输出。现改为在解析管理器中实现 **SkipParse 回落**：直播解析器确认非直播后，自动按注册顺序回落到能解析该链接的平台解析器（视频 b23 → B站视频解析），直播 b23 仍正常出卡片
- 🃏 **QQ 小程序卡片链接提取增强**：新增对结构化卡片 JSON 原文的通用链接扫描（`extract_urls_from_card_raw`），覆盖哔哩哔哩等 QQ 小程序（miniapp）卡片——其分享链接不落在 `qqdocurl / news.jumpUrl` 字段而是内嵌在 JSON 中；扫描结果仍交各平台解析器按域名认领，QQ 自身域名 / 图片 CDN 不会被误解析
- 平台与输出开关按「实际生效的解析器」归因（回落成功的视频短链正确计入 B站视频的开关与卡片显示）

## v1.1.0

- 🔴 **新增 B站直播解析（仅卡片）**：`live.bilibili.com/<房间号>` 直播链接返回仅信息卡片，包含直播间标题、作者、封面、开播状态、在线人数、分区与公告；直播间封面作为卡片主视觉，**不下载 / 不发送任何视频图片媒体**
  - 数据来源为 B站公开接口 `room/v1/Room/get_info` 与直播间页面内嵌作者名，无需登录
  - 支持 `b23.tv/...` 直播短链：直播解析器优先展开认领，确认为直播间后解析；若指向视频等内容则自动交由对应平台解析器处理
  - 在 `解析器与输出模式` 新增独立的「B站直播」平台开关（仅 `关闭 / 全部发送 / 仅文本` 三种）；抖音直播因接口风控暂不支持
- 🐛 **修复 B站直播 b23 短链解析失败**：先前 b23 短链被视频解析器误认领、展开后静默跳过，导致直播无输出并贴失败表情，现已修复
- 💬 **不再对纯卡片结果提示「没有可发送的内容」**：卡片已单独发送并作为解析结果（如 B站直播仅卡片）时不弹该误导提示；仅在所有内容（含卡片）都未发送时才提示
- 🎨 **贴表情反馈与多 Bot 仲裁支持配置**：可分别开关「仲裁」和「成功/失败状态贴图」，并自定义成功 / 失败表情 ID（默认 478「对的对的」/ 479「不对不对」）；仲裁占用表情 289/124 仍协议固定、与 astrbot_plugin_parser 互通
- ✅ **工程与测试**：修复单测脚手架（自举路径替代相对导入，兼容 unittest discover）；新增 GitHub Actions CI（Python 3.10/3.11/3.12，全量编译 + schema/JSON 校验 + 单测），在 `main`/`dev`/`release/**` 触发并支持手动 dispatch；`tests/` 覆盖表情仲裁、链接识别（含 B站直播 / b23）、金样本离线解析、版本一致性，本地单测全绿

## v1.0.2

- 🔧 **版本号统一为单一来源**：新增 `Config.PLUGIN_VERSION` 常量，`main.py` 注册版本改由其引用，避免插件管理面板与 `metadata.yaml` 版本不一致
- 🎨 **贴表情反馈与多 Bot 仲裁支持配置**：在 `参数 → 贴表情反馈与多 Bot 仲裁` 中可分别开关「仲裁」和「成功/失败状态贴图」，并可自定义成功 / 失败表情 ID；仲裁占用表情 289/124 仍协议固定、与 astrbot_plugin_parser 互通
- ✅ 新增内置测试与 CI：`tests/` 单元测试（表情仲裁、链接提取、假响应解析、版本一致性）与 GitHub Actions（全量编译、schema/JSON 校验、测试）

## v1.0.1

- **Steam 卡片强制展示游戏封面**：无论游戏是否有预告片，信息卡片主视觉横幅固定使用 Steam 商店游戏封面（header 横幅图）；走小黑盒完整游戏详情路径时同样生效；截图仍在下方图集区展示

## v1.0.0

首个版本。

### 支持平台

- **B站**：视频、番剧、动态，支持 Cookie 高画质解析与管理员扫码协助登录、热评
- **抖音 / TikTok**：视频、图集，无水印直链
- **快手**：视频、图集
- **微博**：视频、图片、文本、热评
- **小红书**：笔记视频与图片，移动端分享链接去除水印并补全作者头像
- **闲鱼**：商品链接
- **今日头条**：文章、视频、微头条
- **小黑盒**：游戏详情页与 BBS 帖子（签名接口解析，含游戏评分、价格、统计、预告视频）
- **Steam**：商店游戏页（官方接口，可选切换小黑盒完整游戏路径）
- **Twitter / X**：推文文本、图片、视频（FxTwitter 优先，Guest GraphQL 回退）
- **Pixiv**：插画 / 漫画（支持 Cookie 获取原图）

### 核心特性

- 🎴 rika 风格卡片渲染：标准 / 杂志 / 沉浸式 / 信息流四种布局，深 / 浅主题，统计药丸、BV 号简洁展示、封面失败自动兜底；发送顺序为先发卡片，再聚合发送视频与图片
- 🤖 群聊贴表情仲裁（aiocqhttp / OneBot v11）：多 Bot 同群自动竞争解析权、避免重复解析，贴表情反馈解析状态
- 🔁 消息聚合（不聚合 / 全部聚合 / 按条件聚合）、媒体中转、引用链接 ZIP 归档
- 📝 可选 LLM 翻译正文与标题（AstrBot 内置或自定义 OpenAI 兼容接口）
- 🛠️ 每个平台可独立设置输出模式（全部发送 / 仅文本 / 仅富媒体 / 关闭），支持解析频率限制、白黑名单、分平台代理
